<div class="col">
    <label for="recipes"><h3>{{ texts.recipe_list }}</h3></label>
    <ul name="recipes">
        {% for recipe in recipes %}
            <li><a href="{{ url_for('RecipesView:show', id=recipe.id) }}">{{ recipe.name }}</a></li>
        {% endfor %}
    </ul>

    {% if current_user.is_admin %}
	    <h2>Sdílené</h2>
	    <ul name="all_recipes">
	        {% for recipe in all_recipes %}
	            <li><a href="{{ url_for('RecipesView:show', id=recipe.id) }}">{{ recipe.name }}</a></li>
	        {% endfor %}
	    </ul>
    {% endif %}


</div>
