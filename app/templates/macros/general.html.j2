{% macro link_or_name(object) %}

	{% if not print %}
		{{ link_to(object) }}
	{% else %}
		{{ object.name }}
	{% endif %}
	
{% endmacro %}

{% macro list_of_links(items) %}
    <ul>
    {% for item in items %}
        <li>{{ link_or_name(item)}} </li>
    {% endfor %}
    </ul>	
{% endmacro %}