{% extends "base.html.j2" %}
{% block title %} nové heslo pro {{ username }} {% endblock %}

{% block content %}
<form action="{{ url_for('PasswordRecoveryView:post_token', token=token) }}" method="post" class="form-group form-control" accept-charset="UTF-8" data-turbo="false">

    {{ form.csrf_token }}

    {{ form.password.label }}
    <div class="form-group" data-controller="see-password" data-see-password-target="passwordDiv">
        {{ render_field(form.password, has_label=False) }}
    </div>

    {# {{ render_field(form.recaptcha) }} #}
    {{ form.submit(class_='btn btn-primary') }}
</form>
{% endblock %}

