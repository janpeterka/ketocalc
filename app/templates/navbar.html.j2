<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    {% include '_navbar_brand.html.j2' %}
    
    <button class="navbar-toggler" type="button"
            data-toggle="collapse" data-target=".navbar-collapse"
            aria-controls="navbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div id="navbar" class="collapse navbar-collapse">

      <ul class="navbar-nav navbar-center">
        <a class="nav-link active" href="{{ url_for('IngredientView:index') }}"> suroviny </a>
        <a class="nav-link active" href="{{ url_for('RecipeView:index') }}"> recepty </a>
        <a class="nav-link active" href="{{ url_for('DietView:index') }}"> diety </a>
        <a class="nav-link active" href="{{ url_for('DailyPlanView:index') }}"> denní plán {{ icon("daily") }} </a>
        <a class="nav-link active" href="{{ url_for('SimpleCalculatorView:index') }}"> jednoduchá {{ icon("calculator") }} </a>
        <a class="nav-link active" href="{{ url_for('CookbookView:index') }}"> veřejné recepty </a>
        {% if current_user.is_admin %}{% include '_navbar_admin.html.j2' %}{% endif %}
      </ul>

      <ul class="nav navbar-nav navbar-right ml-auto">
        <li class="nav-item dropdown">
          <a class="nav-link" href="#" id="navbarDropdown" role="button"
             data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
             {{ icon("more", cursor_class="cursor-clickable") }}
          </a>

          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="{{ url_for('SupportView:feedback') }}"> Připomínky </a>
          </div>
        </li>

        <li class="nav-item dropdown mr-3">
          <a class="nav-link" href="#" id="navbarDropdown" role="button"
             data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
             <i class="fa fa-question-circle"></i>
          </a>

          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="{{ url_for('SupportView:terms') }}"> Podmínky použití </a>
            <a class="dropdown-item" href="{{ url_for('SupportView:privacy') }}"> Osobní data </a>
            <a class="dropdown-item" href="https://github.com/janmpeterka/ketocalc" target="_blank"> {{ icon("github") }} </a>
            <a class="dropdown-item" href="{{ url_for('SupportView:facebook_redirect') }}" target="_blank" > {{ icon("facebook") }} </a>
          </div>
        </li>

{#         <li class="nav-item mr-4">
          {% include '_navbar_facebook.html.j2' %}
        </li> #}

        <li class="nav-item dropdown">
          <a class="nav-link" href="#" id="navbarDropdown" role="button"
             data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
             {{ icon("user", cursor_class="cursor-clickable", class="text-larger") }}
          </a>

          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="{{ url_for('UserView:show') }}"> Profil </a>
            <a class="dropdown-item" href="/logout"> Odhlásit se </a>
          </div>
        </li>
      {#   <li class="nav-item">
          <a class="nav-link" href="/logout">Odhlásit se</a>
        </li> #}
      </ul>
    </div>
  </div>
</nav>
